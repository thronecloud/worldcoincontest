[local]
# M3 Pro 18GB RAM configuration
beam_size = 50
seeds = 500
parallel_chunks = 8
patience = 10
iterations = 200
description = "Optimized for M3 Pro with 18GB RAM - 8 parallel"

[local_max]
# M3 Pro maximum performance
beam_size = 100
seeds = 200
parallel_chunks = 10
patience = 8
iterations = 150
description = "M3 Pro max performance - uses 10 cores"

[prod]
# Ubuntu 32GB RAM server configuration
beam_size = 200
seeds = 5000
parallel_chunks = 8
patience = 10
iterations = 500
description = "Optimized for Ubuntu server with 32GB RAM"

[prod_ultra]
# Maximum RAM usage for 32GB server
beam_size = 300
seeds = 2000
parallel_chunks = 8
patience = 8
iterations = 300
description = "Ultra mode - trades RAM for quality (uses ~4GB)"

[prod_max]
# EXTREME RAM usage - maximum quality
beam_size = 500
seeds = 1000
parallel_chunks = 8
patience = 5
iterations = 200
description = "MAX mode - uses ~8GB RAM for ultimate quality"

[prod_mega]
# MEGA RAM mode - for finding 3201+
beam_size = 1000
seeds = 500
parallel_chunks = 8
patience = 5
iterations = 150
description = "MEGA mode - uses ~16GB RAM (50% of 32GB) - 8 cores"

[test]
# Quick testing configuration
beam_size = 20
seeds = 10
parallel_chunks = 2
patience = 5
iterations = 50
description = "Quick testing mode"
